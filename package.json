{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix packages/frontend",
    "build": "npm run build --prefix packages/frontend",
    "start": "npm run preview --prefix packages/frontend",
    "test": "npm test --prefix packages/frontend",
    "install": "npm install --prefix packages/frontend",

    "clean": "rimraf packages/*/node_modules packages/*/dist",

    "lint": "npm run lint --prefix packages/frontend",

    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "rimraf": "^5.0.5"
  }
}
